# Start Frontend - Bypass PowerShell execution policy issue
# Use npm.cmd instead of npm to avoid PowerShell script issues

Write-Host "Starting Frontend (Expo)..." -ForegroundColor Green
npm.cmd start
