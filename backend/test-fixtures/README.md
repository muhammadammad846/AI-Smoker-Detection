# Test fixtures for detection

- **test.jpg** – Used by detection tests. If missing, the Python test creates a minimal 640×640 image here.
- For best results, add an image containing a person or smoking-related content (same as your training data) and name it `test.jpg` or pass its path to `run_detection_test.py path/to/image.jpg`.
