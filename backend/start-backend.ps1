# Start Backend - Bypass PowerShell execution policy issue
# Use npm.cmd instead of npm to avoid PowerShell script issues

Write-Host "Starting Backend Server..." -ForegroundColor Green
npm.cmd start
